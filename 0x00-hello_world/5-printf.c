#include <stdio.h>

/**
 * main-prints with proper grammar
 * but outcome is a piece of art,
 * Return: 0 if exited properly, not zero instead of it
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,")
	return (0);
}
